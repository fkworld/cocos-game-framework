const { ccclass, property } = cc._decorator

/**
 * 框架文件：demo-panel
 * - 所有的panel的代码格式请参考这个demo
 * - 如果需要自定义显示方式，则需要添加show()和hide()方法，则在实际过程中会调用对应的方法
 * - show()和hide()方法的原理为替代MPanel.show()，需要具体实现，包括node.active
 */
@ccclass
class PanelDemo extends cc.Component {
    // show() { }

    // hide() { }
}