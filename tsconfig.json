// 特别注意：json严格说不可添加注释，这里作为ts的配置文件使用可以添加注释
{
    "compilerOptions": {
        // 文档错误参考：http://docs.cocos.com/creator/manual/zh/scripting/typescript.html
        // 2.3.3版本，tsconfig.ts会直接影响编译结果，而非文档中所说的不影响
        // 因此需要设置为ES5和CommonJS
        "target": "ES5",
        "module": "CommonJS",
        "lib": [
            "DOM",
            // 注意，此处使用ESNext不代表完全可以使用新语法，与编辑器使用的ts版本有关，但并不知道使用的版本是什么
            "ESNext",
        ],
        "experimentalDecorators": true,
        "skipLibCheck": true,
        "downlevelIteration": true,
        "esModuleInterop": true,
        // 允许umd模块直接使用，针对插件脚本
        "allowUmdGlobalAccess": true,
    },
    "exclude": [
        "node_modules",
        "library",
        "local",
        "temp",
        "build",
        "settings"
    ]
}
